import { AiOutlineLogin } from 'react-icons/ai';

const LoginBtn = () => {
  return (
    <button className='ml-auto text-gray-100 text-5xl login__hover-ani'>
      <AiOutlineLogin />
    </button>
  );
};

export default LoginBtn;
